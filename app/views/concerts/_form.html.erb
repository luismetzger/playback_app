<div class="container">

  <%= form_for(concert) do |f| %>
      <% if concert.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

        <ul>
        <% concert.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :date %>
      <%= f.date_field :date %>
    </div>

    <div class="field">
      <%= f.label :band %>
      <%= f.text_field :band %>
    </div>
    <div class="field">
      <%= f.label :venue%>
      <%= f.text_field :venue %>
    </div>
    <div class="field">
      <%= f.label :notes %>
      <%= f.text_area :notes %>
    </div>
     <div class="field">
      <%= f.text_field :user_id, :value => session[:user_id], :type=>"hidden" %>
    </div>
    
    <input html="{:class=>&quot;form-control&quot;}" type="file" name="paperclip_image[asset]" id="paperclip_image_asset">
    
    <div class="actions">
      <%= f.submit %>
    </div>


  <% end %>



</div>
